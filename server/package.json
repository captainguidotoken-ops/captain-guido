{
  "name": "cgt-admin-server",
  "version": "1.0.0",
  "description": "Captain Guido Token — admin proxy server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.4.1",
    "helmet": "^8.1.0",
    "node-fetch": "^3.3.2"
  }
}
